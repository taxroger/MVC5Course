@model DateTime

@Html.TextBox("", Model.ToString("yyyy-MM-dd"), new { @class = "form-control text-box single-line valid" }) 

<script>
  $( function() {
      $("#@Html.Id("")").datepicker({
          dateFormat: "yy-mm-dd"
      });
  } );
</script>