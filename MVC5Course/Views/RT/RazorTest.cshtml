@model int[]

@for (int i = 0; i < Model.Length; i++)
{
    @Model[i];
}

@for (int i = 0; i < Model.Length; i++)
{
    @(Model[i] + " ")
}

<ul>
    @for (int i = 0; i < Model.Length; i++)
    {
  <li>@(Model[i])</li>
    }
</ul>